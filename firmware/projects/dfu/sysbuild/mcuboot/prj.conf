# 비워 둔다.
#
# 이 폴더가 있으면 MCUboot 이미지의 애플리케이션 설정 폴더가 여기로 바뀐다.
# 즉 이 파일이 MCUboot 기본 prj.conf 를 "대체" 한다. 내용을 여기 넣으면
# CONFIG_FLASH 같은 기본값까지 사라진다 (실제로 겪음 — docs/18_dfu.md).
# 설정은 boards/<보드타깃>.conf 에 넣는다. nrf/samples/dfu/smp_svr 와 같은 구조다.
