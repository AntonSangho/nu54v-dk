CONFIG_GPIO=y

# 부팅 메시지 출력 (VCOM1 / uart20, 115200)
CONFIG_PRINTK=y

# 저전력 : 사용하지 않는 주변장치(UART 등)는 자동 suspend
CONFIG_PM_DEVICE=y
CONFIG_PM_DEVICE_RUNTIME=y

# 디버깅 편의 (-Og, 스레드 정보) - 소비전류 측정/양산 시에는 끈다
CONFIG_DEBUG_OPTIMIZATIONS=y
CONFIG_DEBUG_THREAD_INFO=y
